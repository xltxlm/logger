version: '2'
services:
    php:
      image: registry.cn-hangzhou.aliyuncs.com/xialintai/php:latest
      volumes:
        - ../../:/logger/
      entrypoint: tail -f /root/.bashrc
      networks:
        comm:
          aliases:
            - php

networks:
    comm: