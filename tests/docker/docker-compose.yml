version: '2'
services:
    php:
      image: phplog
      volumes:
        - ../../:/logger/
      entrypoint: tail -f /root/.bashrc
      networks:
        comm:
          aliases:
            - php

networks:
    comm: